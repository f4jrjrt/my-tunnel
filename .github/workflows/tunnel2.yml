    - name: Create server config
      run: |
        SERVER_KEY=$(sudo cat /etc/wireguard/server.key)
        CLIENT_PUB=$(sudo cat /etc/wireguard/client.pub)
        sudo tee /etc/wireguard/wg0.conf > /dev/null << 'EOF'
[Interface]
PrivateKey = SERVER_KEY_PLACEHOLDER
Address = 10.0.0.1/24
ListenPort = 51820
PostUp = iptables -A FORWARD -i wg0 -j ACCEPT; iptables -t nat -A POSTROUTING -o eth0 -j MASQUERADE
PostDown = iptables -D FORWARD -i wg0 -j ACCEPT; iptables -t nat -D POSTROUTING -o eth0 -j MASQUERADE

[Peer]
PublicKey = CLIENT_PUB_PLACEHOLDER
AllowedIPs = 10.0.0.2/32
EOF
        sudo sed -i "s/SERVER_KEY_PLACEHOLDER/$SERVER_KEY/g" /etc/wireguard/wg0.conf
        sudo sed -i "s/CLIENT_PUB_PLACEHOLDER/$CLIENT_PUB/g" /etc/wireguard/wg0.conf
